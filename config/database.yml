# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
default: &default
 adapter: postgresql
 pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
 username: postgres
 password: postgres
 timeout: 5000
#
development:
  <<: *default
  host: localhost
  database: pizzaria-sin5005_dev


# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test: &test
 <<: *default
 host: localhost
 # database: cronograma_de_estudo_test
 database: travis_ci_test

production:
 <<: *default
 database: pizzaria-sin5005


cucumber:
  <<: *test
