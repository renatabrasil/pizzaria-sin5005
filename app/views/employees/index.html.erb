
<h1>Listar Funcionários</h1>
<%= link_to 'Cadastrar Funcionário', new_employee_path %>
<table>
  <tr>
    <th>Nome</th>
    <th>Cargo</th>
    <th>Salário</th>
    <th>Carga Horária</th>
    <th>Telefone</th>
    <th colspan="3"></th>
  </tr>

  <% @employees.each do |employee| %>
    <tr>
      <td><%= employee.name %></td>
      <td><%= employee.office %></td>
      <td><%= employee.salary %></td>
      <td><%= employee.workload %></td>
      <td><%= employee.telephone %></td>
      <td><%= link_to 'Visualizar', employee_path(employee) %></td>
      <td><%= link_to 'Editar', edit_employee_path(employee) %></td>
      <td><%= link_to 'Excluir', employee_path(employee),
              method: :delete,
              data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
</table>
