<h1>Listar Funcionários</h1>
<%= link_to 'Cadastrar Funcionário', new_employee_path, class: "btn btn-primary" %>
<div class="table-responsive">
  <table class="table table-striped">
    <thead>
      <tr>
        <th>Nome</th>
        <th>Cargo</th>
        <th style="text-align: right">Salário (R$)</th>
        <th style="text-align: center">Carga Horária (h/semana)</th>
        <th style="text-align: center">Telefone</th>
        <th colspan="3" width="18%"></th>
      </tr>
    </thead>
    <tbody>

    <% if @employees.empty? %>
      <%= render partial: 'shared/empty_data', :locals => {:total_columns => 8} %>
    <% end %>

    <% @employees.each do |employee| %>
      <tr>
        <td><%= employee.name %></td>
        <td><%= employee.office %></td>
        <td style="text-align: right"><%= number_to_currency(employee.salary, unit: "", separator: ",", delimiter: ".") %></td>
        <td style="text-align: center"><%= employee.workload %></td>
        <td style="text-align: center"><%= employee.telephone %></td>
        <td><%= link_to 'Visualizar', employee_path(employee) %></td>
        <td><%= link_to 'Editar', edit_employee_path(employee) %></td>
        <td><%= link_to 'Excluir', employee_path(employee),
                method: :delete,
                data: { confirm: 'Are you sure?' } %></td>
      </tr>
    </tbody>
    <% end %>
  </table>
</div>
